<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <div class="text-center">
        <% if params[:plan] == '2'%>
          <h1>DevMatch Pro</h1>
          <p>Suscribirse por $10/mes para acceder a la comunidad y datos de contacto</p>
        <% else %>
          <h1>DevMatch Basic</h1>
          <p>Acceso a las funciones básicas de la comunidad</p>
        <% end %>
      </div>
      
      <div class="well">
        <h2>Registrarse</h2>
        
        <% if params[:plan] == '2' %>
          <%= render "pro_form" %>
        <% else %>
          <%= render "basic_form" %>
        <% end %>
        
                
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>